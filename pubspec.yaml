name: sanrakshaa
description: "A new Flutter project for women's safety."

publish_to: 'none'

version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase core and authentication
  # firebase_core: ^2.0.0
  # firebase_auth: ^5.0.0

  # Optional: Google Sign-In (if you plan to use Google authentication)
  google_sign_in: ^5.0.0
  circular_countdown_timer: ^0.2.4
  # Optional: Flutter SVG support for handling vector assets
  flutter_svg: ^2.0.0
  firebase_core: ^3.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true


  # Optional: Include fonts if custom fonts are used
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  #       - asset: assets/fonts/Roboto-Bold.ttf
  #         weight: 700